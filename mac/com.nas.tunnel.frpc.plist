<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  Mac mini 端 —— frpc 反向隧道客户端开机自启（launchd）

  ⚠️ 这是「手工部署」用的参考模板。直接跑 deploy-nas-tunnel-mac.sh 的话，
     脚本会按当前登录用户自己生成并加载这份 plist，你不需要手工用它。

  ⚠️ frpc 必须在 hysteria 之后启动（脚本里就是这么安排的）：
     它要把「CONNECT 127.0.0.1:7000」交给 hysteria 的 socks5 入站。

  手工使用时需要改两处：
    1) 把 __RUN_USER__ 换成你的登录用户名
    2) 确认 /usr/local/var/log/nas-tunnel/ 属于该用户
       另外 frpc.toml 里的 log.to 也要指向该目录（服务器生成的模板写的是 /var/log，普通用户写不进去）

  安装：
    sudo cp mac/com.nas.tunnel.frpc.plist /Library/LaunchDaemons/
    sudo chown root:wheel /Library/LaunchDaemons/com.nas.tunnel.frpc.plist
    sudo chmod 644        /Library/LaunchDaemons/com.nas.tunnel.frpc.plist
    sudo launchctl bootstrap system /Library/LaunchDaemons/com.nas.tunnel.frpc.plist
-->
<plist version="1.0">
<dict>
  <key>Label</key>
  <string>com.nas.tunnel.frpc</string>

  <key>ProgramArguments</key>
  <array>
    <string>/usr/local/bin/frpc</string>
    <string>-c</string>
    <string>/usr/local/etc/nas-tunnel/frpc.toml</string>
  </array>

  <key>UserName</key>
  <string>__RUN_USER__</string>

  <key>RunAtLoad</key>
  <true/>
  <key>KeepAlive</key>
  <true/>
  <key>ProcessType</key>
  <string>Background</string>

  <key>StandardOutPath</key>
  <string>/usr/local/var/log/nas-tunnel/frpc.log</string>
  <key>StandardErrorPath</key>
  <string>/usr/local/var/log/nas-tunnel/frpc.log</string>
</dict>
</plist>
